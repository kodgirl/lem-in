#include <limits.h>

using namespace std;

const int V = 6;

void    Dijkstra(int gr[V][V], int st)
{
	int distance[V];
	int count;
	int index;
	int i;
	int u;
	int m;
	m = st + 1;
	bool visited[V];

	i = 0;
	while (i < V)
	{
		distance[i] = INT_MAX;
		visited[i] = false;
	}

	int     main(void)
	{
		int start;
		int GR[V][V] = {
				0, 1, 4, 0, 2, 0
		};
	}


}